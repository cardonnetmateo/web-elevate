export interface PricingCardProps {
  title: string;
  subtitle: string;
  price: string;
  priceLabel: string;
  recurring: string;
  features: string[];
  buttonText: string;
  buttonHref: string;
  featured?: boolean;
}
