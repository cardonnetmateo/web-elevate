<section class="relative py-24 px-6 bg-black overflow-hidden">
  
  <div class="absolute inset-0 bg-linear-to-b from-indigo-500/10 via-black to-cyan-500/10"></div>
  <div class="absolute inset-0 bg-radial from-indigo-500/10 at center to-transparent"></div>
  
  <div class="relative z-10 max-w-4xl mx-auto text-center">
    
    <div class="fade-in">
      <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
        Dejá de perder <span class="bg-linear-to-r from-red-400 to-orange-400 bg-clip-text text-transparent">clientes y tiempo</span>
      </h2>
      <p class="text-white/60 text-lg md:text-xl max-w-2xl mx-auto mb-10 leading-relaxed">
        Cada minuto que perdés en procesos manuales es plata que se va. La automatización reemplaza el trabajo repetitivo para que vos te enfoques en lo que importa: hacer crecer tu negocio.
      </p>
    </div>

    <div class="fade-in flex flex-col sm:flex-row justify-center gap-4">
      <a href="#contacto" class="group relative px-8 py-4 rounded-xl bg-white text-black font-bold text-lg hover:shadow-[0_0_60px_rgba(255,255,255,0.2)] transition-all duration-300 hover:-translate-y-1 overflow-hidden">
        <span class="relative z-10">Agendar llamada gratis</span>
        <div class="absolute inset-0 bg-linear-to-r from-indigo-400 to-cyan-400 opacity-0 group-hover:opacity-10 transition-opacity"></div>
      </a>
      <a href="#pricing" class="px-8 py-4 rounded-xl border border-white/20 text-white font-bold text-lg hover:bg-white/5 hover:border-white/30 transition-all duration-300 hover:-translate-y-1">
        Ver planes y precios
      </a>
    </div>

    <div class="fade-in mt-12 flex items-center justify-center gap-6 text-white/40 text-sm">
      <div class="flex items-center gap-2">
        <svg class="w-4 h-4 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
        <span>Sin compromiso</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-4 h-4 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
        <span>30 min de llamada</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-4 h-4 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
        <span>Plan personalizado</span>
      </div>
    </div>

  </div>

</section>

<style>
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s ease forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
